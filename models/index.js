const { Contact, Car, contactsJoiSchemas } = require("./contact")
const { User, authJoiSchemas } = require("./user")

module.exports = {
    Contact,
    Car,
    contactsJoiSchemas,
    User,
    authJoiSchemas,
}
